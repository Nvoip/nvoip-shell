name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  shell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sh -n lib/*.sh examples/*.sh Scripts/*.sh
